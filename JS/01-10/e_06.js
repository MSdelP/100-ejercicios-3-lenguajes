// Escribir en JS un programa que declare la variable "d" y le asigne el valor y verifique si esta variable es mayor o menor que 0. 
// Si la variable es mayor a 0, debe imprimir "Positiva", si no lo es debe imprimir "Negativa".


// Declarar la variable y asignarle un valor
let d = 10;  // Puedes cambiar este valor según tus necesidades

// Verificar si la variable es mayor o menor que 0
if (d > 0) {
    console.log("Positiva");
} else if (d < 0) {
    console.log("Negativa");
} else {
    console.log("La variable es igual a 0");
}
