// Crear una lista vacía L
var L = [];

// Agregar enteros a la lista
L.push(10);
L.push(25);
L.push(42);
L.push(69);
L.push(31);

// Agregar cadenas de caracteres a la lista
L.push('jd');
L.push('xa');
L.push('rtx');

// Mostrar la lista después de agregar elementos
console.log("Lista después de agregar elementos:", L);


// En este código, inicializamos una lista vacía L utilizando un array literal ([]).
// Luego, utilizamos el método push() para agregar los enteros y cadenas de caracteres a la lista.
// Finalmente, mostramos la lista después de agregar los elementos utilizando console.log(). La salida esperada sería la lista con los elementos agregados.