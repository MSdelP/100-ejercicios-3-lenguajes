// Variables
let miNombre = "Mario";
let edad = 37;
let lenguaje = "JavaScript";

// Cadena de formato utilizando template literals
let cadenaFormateada = `Me llamo ${miNombre} y tengo ${edad} años. Estoy aprendiendo el lenguaje ${lenguaje}.`;

// Imprimir la cadena formateada
console.log(cadenaFormateada);


// En este ejemplo, utilizamos las comillas invertidas (``) para crear una cadena de formato utilizando template literals.
// Dentro de la cadena, podemos insertar valores de variables directamente utilizando la sintaxis ${variable}.
// Luego, imprimimos la cadena formateada en la consola.