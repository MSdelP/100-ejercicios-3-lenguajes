# Escribir una instrucción que permite crear una lista de números del 1 al 10 utilizando una comprensión de lista en python.

numeros = [i for i in range(1, 11)]

# Esta instrucción utiliza la comprensión de lista para generar una lista que contiene los números del 1 al 10. 
# La función range(1, 11) crea una secuencia de números desde 1 hasta 10 (incluyendo el 1 pero excluyendo el 11), 
# y la comprensión de lista [i for i in range(1, 11)] crea una lista que contiene estos números.